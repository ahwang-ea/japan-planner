{
  "name": "japan-planner",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"node scripts/wait-for-server.cjs && npm run dev -w client\"",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npx -w client vite build && npm run build -w server && cp server/src/lib/schema.sql server/dist/lib/",
    "start": "node server/dist/index.js"
  },
  "workspaces": ["server", "client"],
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
